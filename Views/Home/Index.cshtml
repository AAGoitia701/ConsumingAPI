<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Harry Potter Quiz</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #2e2d2b;
            color: #f5f5f5;
            font-family: 'Georgia', serif;
        }
        .quiz-container {
            max-width: 700px;
            margin: auto;
            padding: 2rem;
            background-color: #333;
            border-radius: 10px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        .quiz-title {
            font-size: 3rem;
            font-weight: bold;
            color: #f7d574;
        }
        .quiz-subtitle {
            font-size: 1.2rem;
            color: #d3c4a1;
            margin-top: 1rem;
        }
        .btn-start {
            background-color: #6b3f07;
            color: #fff;
            font-size: 1.2rem;
            font-weight: bold;
            padding: 0.8rem 1.5rem;
            border-radius: 5px;
            margin-top: 2rem;
        }
        .btn-start:hover {
            background-color: #8b5e3c;
            color: #fff;
        }
        .hp-image {
            max-width: 100%;
            border-radius: 10px;
            margin-top: 1.5rem;
        }
    </style>
</head>
<body>

    <!-- Contenedor principal del quiz -->
    <div class="quiz-container my-5">
        <h1 class="quiz-title">Welcome to the Harry Potter Quiz</h1>
        <p class="quiz-subtitle">Are you ready to test your wizarding world knowledge? Find out if you're a true Potterhead!</p>

        <!-- Imagen de ambientación -->
        <img src="https://www.merchmates.co.uk/wp-content/uploads/2023/08/hogwarts-house-crest.png" alt="Harry Potter Banner" class="hp-image">

        <!-- Botón para iniciar el quiz -->
        <div class="text-center">
            <a class="btn btn-start mt-4" asp-controller="Quiz" asp-action="GetOneViewModel">Start the Quiz</a>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>

